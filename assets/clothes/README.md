# 服装素材说明

服装按分类放入以下目录：

- `hair/`：发型
- `top/`：上衣
- `bottom/`：下装
- `shoes/`：鞋子
- `accessory/`：饰品

每张图片推荐使用 `600 x 800 px` 透明 PNG。只绘制服装本身，人物所在位置要与
`assets/character/body.png` 完全对齐。

新增服装后，请在 `data/clothes.json` 对应分类的 `items` 数组中添加名称和图片路径。
